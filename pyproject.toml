[project]
name = "starx-workbench"
version = "0.1.0"
description = "Starx MVS-1 project skeleton."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
